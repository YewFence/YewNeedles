[tools]
gh = "latest"
gitleaks = "8.30.1"
jq = "latest"
"pipx:git+https://github.com/YewFence/YewNeedles.git" = { version = "latest#subdirectory=packages/zsh-completions-sync", uvx_args = "--no-managed-python" }
python = "latest"
uv = "latest"

[tasks."ops:shell-volumes"]
description = "打开带有简单工具的 shell 以简单管理指定的 named volume"
run = "./volume-tools/volume-shell/open.sh"

[tasks."ops:sftp-volumes"]
description = "将指定的 named volume 暴露为 sftp 服务"
run = "./volume-tools/volume-sftp/open.py"

[tasks."ops:copy-volume"]
description = "在宿主机目录和 Docker 命名卷之间同步内容"
file = "volume-tools/volume-copy/volume_copy.py"

[tasks."ops:relabel-volume"]
description = "原地重建 Docker 命名卷并更新它的 labels"
file = "volume-tools/volume-relabel/volume_relabel.py"
