[project]
name = "yewneedles"
version = "0.1.0"
description = "Local control-plane tooling for Ansible-based infrastructure"
requires-python = ">=3.12"
dependencies = [
  "ansible-core~=2.20.0",
  "ansible-lint~=25.0",
  "passlib~=1.7",
]

[tool.uv]
package = false
